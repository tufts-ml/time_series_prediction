{      
    "name": "{{sheet_name}}",
    "path": "{{csv_filename}}",
    "format": "csv",
    "mediatype": "text/csv",
    "profile": "tabular-data-resource",
    "$schema": "https://frictionlessdata.io/schemas/table-schema.json",
    "schema": {
        "fields": [
            "{{row_template}}"
        ],
        "missingValues": ["", "nan"]
    }
}
